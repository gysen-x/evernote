// const { Notebook } = require('../../db/models');

exports.getNotebooks = async (req, res) => {
  try {
    // const notebooksFromDB = await Notebook.findAll()
  } catch (error) {
    console.log('error: ', error);
  }
};
